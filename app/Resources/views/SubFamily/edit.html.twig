{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>Edit SubFamily {{ subFamily.name }}</h1>
                {{ include('SubFamily/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
